# Full-data compute run

This marker commit triggers the PR-observable `frontier-097-compute-pr` workflow against `agent/frontier-097-campaign`. The workflow runs the full target-encoding frontier, materializes the aligned quad anchor, and evaluates fixed-schedule LightGBM, XGBoost, and CatBoost residual contrasts using the repository's existing public S6E8 mirror.
