name: te-capacity-screen
on:
  push:
    branches: [agent/te-capacity-screen]
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  screen:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Install modeling stack
        run: pip install 'numpy>=2,<3' 'pandas>=2.2,<3' 'scikit-learn>=1.5,<2' 'lightgbm>=4.3,<5' 'xgboost>=2.1,<4'
      - name: Acquire public mirror
        run: |
          git clone --depth 1 https://github.com/s23010843/Predicting-Smartphone-Addiction.git /tmp/s6e8-data
          mkdir -p /tmp/s6e8
          cp /tmp/s6e8-data/input/train.csv /tmp/s6e8/train.csv
      - name: Run screen
        run: python experiments/te_capacity_screen.py --data-dir /tmp/s6e8 --out te_capacity_screen.json --rows 120000 --max-estimators 2000
      - uses: actions/upload-artifact@v4
        with:
          name: te-capacity-screen
          path: te_capacity_screen.json
          retention-days: 7
